[package]
name = "labview-test-library"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
labview-interop = {path = "../labview-interop", default-features = false }

[lib]
crate-type = ["cdylib"]
